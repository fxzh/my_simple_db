# storage
存储引擎：页/文件/缓冲池/记录编解码，当前以堆页追加 + 全表扫描方式存取数据

- 完整设计见 docs/storage-design.md；里程碑路线：M1 堆页追加+全表扫描(无索引/无主键) → M3 B+树聚簇索引(跨页记录届时树内处理) → M4 WAL
- 结构体字段按"不做版本与迁移"约定，只存放当前里程碑实际用到的
- 公开原语须持锁调用(锁在 catalog)，本库内部不加锁
- M1 持久性语义：追加写容忍尾部截断，损坏数据页按尾部截断重建空页，重启后已追加数据仍在；记录不跨页(单行约 4KB 上限，超长拒绝)
